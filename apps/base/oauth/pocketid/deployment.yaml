---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pocketid
  namespace: oauth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pocketid
  template:
    metadata:
      labels:
        app: pocketid
    spec:
      containers:
        - name: pocketid
          image: ghcr.io/pocket-id/pocket-id:v2.14.0
          env:
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: pocketid-secret
                  key: ENCRYPTION_KEY
            - name: APP_URL
              value: https://id.lan.${DOMAIN}
            - name: RELYING_PARTY_ID
              value: id.lan.${DOMAIN}
          ports:
            - containerPort: 1411
          volumeMounts:
            - name: data
              mountPath: /app/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: pocketid-data
