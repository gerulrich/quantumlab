---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubed
  namespace: kube-system
spec:
  interval: 1h
  chart:
    spec:
      chart: kubed
      version: "v0.13.2"
      sourceRef:
        kind: HelmRepository
        name: appscode
        namespace: kube-system
      interval: 1h
